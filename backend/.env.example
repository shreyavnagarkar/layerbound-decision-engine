# Origin allowed to call the API (in addition to the local Vite ports, which
# are always allowed for convenience during development).
FRONTEND_URL=http://localhost:5173

# SQLAlchemy connection string. Defaults to a local SQLite file if unset.
# DATABASE_URL=sqlite:///./layerbound_intake.db
